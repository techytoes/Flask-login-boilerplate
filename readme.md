### Flask JWT-login Boilerplate

#### Steps to run:

* Install requirements:
```
pip install -r requirements.txt
```

* Run the script:
```
python init_db.py
```

This will create database and schema for the app.

* Start the server:
```
python app.py
```